def sum_of_list(numbers):
    total = sum(numbers)
    print("ფუნქციის გარეთ:", total)
    return total

# სიის შექმნა
numbers = [1, 2, 3, 4, 5]

# ფუნქციის გამოძახება და შედეგის დაბეჭდვა
result = sum_of_list(numbers)
print("დაბრუნებული შედეგი:", result)